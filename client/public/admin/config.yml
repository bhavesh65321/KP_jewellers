backend:
  name: github
  repo: bhavesh65321/KP_jewellers
  branch: main

media_folder: client/public/products/images
public_folder: /products/images

collections:
  - name: rings
    label: Rings
    folder: client/public/products/rings
    create: true
    slug: "{{slug}}"
    fields:
      - label: Title
        name: title
        widget: string
